import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppFormStorage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
